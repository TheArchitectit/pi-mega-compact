node_modules/
dist/
!extensions/dashboard-client/dist/
!extensions/dashboard-client/dist/**
*.log
.env
.env.*
.DS_Store

# FORBIDDEN — npm is the ONLY distribution path (PREVENT-DIST-001).
# Never produce or commit a `.tgz` (`npm pack`); tarballs bypass pi's package
# manager and don't propagate to other devices. `npm publish` only.
*.tgz

# Local runtime state for the extension (checkpoints / vector db)
.pi-mega-compact-state/
.pi/
dashboard.json
events.log
dedup-stats.json
bloom.json.gz
sqlite.db
sqlite.db-shm
sqlite.db-wal

# Vendored upstream template (config pieces extracted into .claude/.guardrails)
guardrails-template/
sess_*
sqlite.db

# Local runtime audit/state dir from pi-crew / mega-compact
.crew/

# PGlite vector index (Slice 2) + stray :memory: pgdata artifact from bad dataDir
mega-compact-vector/
:memory:/

/.crew/
/.crew
!.crew/artifacts/
!.crew/graphs/
!.crew/artifacts/.gitkeep
!.crew/graphs/.gitkeep
# agent temp scratch dir
tmp/

# Benchmark results (per-run, not committed)
scripts/benchmark/out/

# Benchmark results + logs (transcript-derived, may contain repo/session data)
scripts/benchmark/*-results.jsonl
scripts/benchmark/*-bench.log
scripts/benchmark/results.json
