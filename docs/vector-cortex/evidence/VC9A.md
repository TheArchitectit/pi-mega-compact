# VC9A Evidence

Status: implementer-complete
Implementation commits/sub-sprint gates: single commit `feat(vc9a): ...` (see git log); full gate run on the working tree (build / test / lint / regression / guardrails / conformance / docs-check / scope-check / evidence-check / dashboard-client tsc+build).
Contract review: pending — reviewer attestation below is marked pending until the controller (Opus) reads every touched file, runs the mutation scan, and re-runs the gates.
Changed production/tests/docs: `src/config/vector-cortex.ts`, `src/config/vector-cortex-vc9a.ts`, `src/config.ts`, `extensions/dashboard-server/setup-cortex-blockers.ts`, `extensions/dashboard-server/api-contracts/setup-cortex.ts`, `extensions/dashboard-server/routes-setup-cortex.ts`, `extensions/dashboard-server/routes-setup-cortex.test.ts`, `extensions/dashboard-server/routes.ts`, `extensions/dashboard-server/route-dispatch.ts`, `extensions/dashboard-server/api-contracts/endpoints/registry-ext.ts`, `extensions/dashboard-server/routes-rag-settings-vector-cortex.ts`, `extensions/dashboard-server/api-contracts.test/endpoints-registry.test.ts` (count bump 49→50, forced deviation), `scripts/vc9-setup-dashboard/gen-fixtures.mjs`, `src/vector-cortex/vc9a-acceptance.test.ts`, `scripts/vector-cortex-docs-check.mjs`, `conformance/vector-cortex/v2/schemas/setup-cortex-fixture.schema.json`, `conformance/vector-cortex/v2/setup-dashboard/SETUP-CORTEX-001.json`..`.009.json`, `conformance/vector-cortex/v2/manifest.json`, `docs/vector-cortex/sprints/VC9A-setup-cortex-status.md`, `docs/vector-cortex/evidence/VC9A.md`.
Fixtures and corpus digests: 9 `SETUP-CORTEX-001..009` fixtures + the `setup-cortex-fixture` schema registered in the v2 manifest (781 fixtures canonical); generated by `scripts/vc9-setup-dashboard/gen-fixtures.mjs` and committed.
Migration: pure sprint — no migration.
A/B/C and independence evidence: A reader-only full projection (flag-on); B payload-free digest-prefix projection (never bytes); C the offset VC8C-era `{enabled:false, mode:"C", status:"off"}` shape forced by `MEGACOMPACT_VC9A=0`. A/B share the encoder asset seams; C is produced purely by the flag gate. The 9 fixtures pin each of the three modes + the flag-off shape + the canonical blockers.
Commands and verbatim summaries: see Gate Results + unit-test claims below.

## File sizes

All touched files under their extension (500) / src (500) / scripts (no cap) / docs (500) hard limits; the one pre-existing near-limit file `api-contracts/endpoints/registry.ts` (496) is UNTOUCHED by this sprint — the endpoint registered in the additive `registry-ext.ts` seam (78) to stay under the 500-line hard limit:

- `src/config/vector-cortex-vc9a.ts` (31)
- `src/config/vector-cortex.ts` (299)
- `src/config.ts` (195)
- `extensions/dashboard-server/setup-cortex-blockers.ts` (73)
- `extensions/dashboard-server/api-contracts/setup-cortex.ts` (65)
- `extensions/dashboard-server/routes-setup-cortex.ts` (167)
- `extensions/dashboard-server/routes-setup-cortex.test.ts` (131)
- `extensions/dashboard-server/routes.ts` (59)
- `extensions/dashboard-server/route-dispatch.ts` (152)
- `extensions/dashboard-server/api-contracts/endpoints/registry-ext.ts` (78)
- `extensions/dashboard-server/api-contracts/endpoints/registry.ts` (496) — untouched, no endpoint added here
- `extensions/dashboard-server/routes-rag-settings-vector-cortex.ts` (202)
- `extensions/dashboard-server/api-contracts.test/endpoints-registry.test.ts` (194) — forced deviation: `EXPECTED_ENDPOINT_COUNT` 49→50 for the new `setup-cortex-status` endpoint
- `scripts/vc9-setup-dashboard/gen-fixtures.mjs` (303)
- `src/vector-cortex/vc9a-acceptance.test.ts` (219)
- `scripts/vector-cortex-docs-check.mjs` (183)
- `docs/vector-cortex/sprints/VC9A-setup-cortex-status.md` (53)
- `docs/vector-cortex/evidence/VC9A.md` (this record)
- `conformance/vector-cortex/v2/schemas/setup-cortex-fixture.schema.json` (1, canonical JSON)
- `conformance/vector-cortex/v2/setup-dashboard/SETUP-CORTEX-001.json`..`.009.json` (1 each, canonical JSON)

`registry-ext.ts` grows to 78 (was 67) — well under the 400 soft / 500 hard extension limits; no delegate-shell split required.

## Gate Results

| Gate | Result |
|------|--------|
| `npm run build` | PASS |
| `node --test dist/vector-cortex/vc9a-acceptance.test.js` | PASS (7/7) |
| `npm test` | PASS (see below) |
| `npm run lint` | PASS (tsc + guardrails + semantic) |
| `python3 scripts/regression_check.py --all` | PASS (0 hard violations in changed files; only pre-existing soft warnings) |
| `node scripts/guardrails-scan.mjs` | PASS (pi pattern scan clean) |
| `python3 scripts/log_failure.py --list` | PASS (only resolved entries) |
| `node scripts/vector-cortex-conformance.mjs --check` | PASS (781 fixtures canonical) |
| `node scripts/vector-cortex-docs-check.mjs` | PASS (30 sprints / 9 phases) |
| `node scripts/vector-cortex-scope-check.mjs VC9A <commit>` | PASS (all committed files in ownership + seams) |
| `node scripts/vector-cortex-evidence-check.mjs VC9A` | PASS (see below) |
| `git diff --check` | PASS |
| `cd extensions/dashboard-client && npm run typecheck` / `npm run build` | PASS (no client change; verified) |

## VC9A unit/acceptance tests

Acceptance aggregator (fixtures-driven, flag-agnostic):

`node --test dist/vector-cortex/vc9a-acceptance.test.js` → `ℹ tests 7` `ℹ pass 7` `ℹ fail 0`

`MEGACOMPACT_VC9A=0 node --test dist/vector-cortex/vc9a-acceptance.test.js` → `ℹ tests 7` `ℹ pass 7` `ℹ fail 0` (flag-off parity — same suite green under both flag states)

Route unit tests (spawn-and-fetch, exact source listed in the spec):

`node --test dist/extensions/dashboard-server/routes-setup-cortex.test.js` → `ℹ tests 4` `ℹ pass 4` `ℹ fail 0`

Full `npm test` gate: PASS on the merged tree (baseline grows with the 2 new test files). `node scripts/vector-cortex-evidence-check.mjs VC9A` runs the acceptance aggregator for real and confirms the counts above; the line-count + fixture-count claims are re-derived from the tree.

## Evaluation

- `GET /api/setup-cortex-status` is reader-only: it computes encoder facts from the committed asset (manifest digest prefix + verify result) and returns the mode/verdict/blockers/encoder-health/status projection — never payload bytes, prompts, or ledger (EVAL-REDACT-002). No mutation surface (non-GET → 405).
- Mode A (verified asset), B (present but demoted), C (absent) all encoded in the 9 fixtures; the flag-off shape `{enabled:false, mode:"C", status:"off"}` is byte-identical to the VC8C-era predecessor.
- Blockers come from the static manifest `setup-cortex-blockers.ts` (HG-1 / HG-3 + transformers.js WASM candidate / HG-4 / HG-5); the opset HG-2 blocker is REMOVED per the 2026-08-05 research. No string literals in the route file.

## Offline/network/asset/platform evidence

Reader-only local filesystem reads (PREVENT-PI-004, audited with `// guardrails-allow` inline annotations). No network, no subprocess spawn, no writes. Platform-neutral: mode resolves per `detectPlatform()`; the acceptance test adapts to the committed asset on the current host.

## Rollback/downgrade rehearsal

`MEGACOMPACT_VC9A=0` — flag-off. The endpoint returns `{enabled:false, mode:"C", status:"off"}` with empty blockers, byte-identical to the VC8C-era shape. No schema/state change; evidence retained.

## Residual risks

- The main `scripts/vector-cortex-gen-fixtures.mjs` regeneration (`rmSync` of the v2 root) does not yet know about the VC9A `setup-dashboard/` fixtures or the `setup-dashboard`/`setup-cortex-fixture`/`VC9A` manifest header tokens. A full regeneration by that script would drop these until it is taught about the new seam. This is a latent cross-cutting reconciliation for the controller (not a VC9A gate failure — the committed fixtures + manifest are self-consistent and pass `--check`).
- `api-contracts/endpoints/registry.ts` is untouched at 496 lines but remains near its 500 hard limit; future additive groups should continue to use `registry-ext.ts`.
- Reviewer attestation pending — the controller must read every file, run the mutation scan, and re-run the gates before flipping Status to reviewer-accepted.

## Reviewer attestation

Name/date/status: pending (Claude Opus controller to review). Acceptance checklist deltas: (1) endpoint registered in `registry-ext.ts` (not `registry.ts`) — a code-following deviation from the plan's `registry.ts (additive)` spelling because `registry.ts` sits at 496 and a new group crosses the 500-line hard limit; declared in the sprint spec's Production ownership field for ratification. (2) `VC9A_ENABLED` lives in the `vector-cortex-vc9a.ts` config sibling (delegate-shell) to keep `vector-cortex.ts` under 300; re-exported by `vector-cortex.ts` + root `src/config.ts` so no import path changes. (3) Flag-off parity is exercised by the route unit test (off case) + the evidence-check flag-off parity run; the acceptance aggregator itself is flag-agnostic so the same suite is green both ways. (4) **Forced deviation:** `api-contracts.test/endpoints-registry.test.ts` (NOT in the plan's dev-ownership set) is edited — `EXPECTED_ENDPOINT_COUNT` 49→50 — because the mandatory `npm test` gate hard-codes the registry entry count and the additive `setup-cortex-status` endpoint raises it to 50. Recorded in the spec's Production ownership field for ratification; this is a mechanical registry-count reconciliation, not scope drift.
