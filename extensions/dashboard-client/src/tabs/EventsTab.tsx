/**
 * dashboard-client/src/tabs/EventsTab.tsx — Events tab (B1 stub).
 *
 * SPRINT-C1: live SSE event stream via useSSE.
 */

import type React from 'react';

export default function EventsTab(): React.ReactElement {
  return (
    <div className="tab-stub">
      <strong>Events</strong> (B1 stub)
      {/* SPRINT-C1-REMAINING: live SSE stream via useSSE */}
    </div>
  );
}
