/**
 * dashboard-client/src/tabs/ReposTab.tsx — Repos tab (B1 stub).
 *
 * SPRINT-C2: repos table, drill-down into a single repo snapshot.
 */

import type React from "react";

export default function ReposTab(): React.ReactElement {
	return (
		<div className="tab-stub">
			<strong>Repos</strong> (B1 stub)
			{/* SPRINT-C2-REMAINING: repos table via /api/repos */}
		</div>
	);
}
