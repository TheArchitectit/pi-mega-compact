{"$schema":"https://json-schema.org/draft-07/schema#","description":"Common structure every DEDUP-ATTR fixture validates against. `scenario` pins the failure-triad/reasoning case the fixture documents (non-empty / empty / flag-off / pure); `total_decisions` pins the rolled-up window decision count; `shares_sum_to_one` pins whether the l0+l1+l2 shares of that window sum to exactly 1.0; `expected_status` pins the shared deriveVcStatus status the endpoint reports for the case; `kind` pins the algorithm.","properties":{"assertion":{"type":"string"},"expected_status":{"enum":["live","awaiting_data","off"],"type":"string"},"id":{"type":"string"},"kind":{"enum":["dedup-attribution"],"type":"string"},"producer":{"type":"string"},"scenario":{"enum":["non-empty","empty","flag-off","pure"],"type":"string"},"schema":{"type":"string"},"shares_sum_to_one":{"type":"boolean"},"total_decisions":{"type":"integer"}},"required":["id","producer","assertion","kind","scenario"],"title":"DEDUP-ATTR tier-attribution fixture envelope","type":"object"}
